import 'package:flutter/material.dart';

class AcademicDetails extends StatelessWidget {
  const AcademicDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Academics Deatils"),
      ),
    );
  }
}