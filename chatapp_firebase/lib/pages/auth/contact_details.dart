import 'package:flutter/material.dart';

class ConactDetails extends StatelessWidget {
  const ConactDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Contact Details"),
      ),
    );
  }
}