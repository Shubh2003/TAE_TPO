import 'package:flutter/material.dart';

class IntershipDetails extends StatelessWidget {
  const IntershipDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Intership Deatils"),
      ),
    );
  }
}