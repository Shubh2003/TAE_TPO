import 'package:flutter/material.dart';

class TpoHomePage extends StatelessWidget {
  const TpoHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
        child: Text("TPO Home Page"),
      ),
    );
  }
}